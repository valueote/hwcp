int main(){
  int sum = 0;
  int i = 0;

  for (i = 0; i < 5; i++) {
     sum = sum + i;
  }
  putint(sum); 
  int newline = 10;
  putch(newline);

  int diff = 10;
  for (i = 5; i > 0; i-- ) {
    diff = diff - i;
  }
  putint(diff);  
  return 0;
}
